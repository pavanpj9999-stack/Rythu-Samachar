// Firebase configuration is disabled as we are running in Offline Mode (IndexedDB)
// due to environment setup issues.

export const db = null as any;
export const storage = null as any;
export const auth = null as any;
export const app = null as any;

export default null;